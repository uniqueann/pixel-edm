# P3 活动管理交付清单

更新日期：2026-09-11。P3-0 至 P3-4 已完成工程实现、本地验证和云端数据库迁移，A 版从活动草稿到 CSV 导出的管理闭环已经形成。

## P3-0 设计基线

- [x] 活动状态预留 `draft`、`queued`、`sending`、`completed`、`completed_with_errors`、`failed`；P3-1 只允许业务 RPC 操作 `draft`。
- [x] 管理员和编辑者可创建、读取完整草稿、编辑、归档及恢复；查看者只能读取不含变量和邮件正文的活动摘要。
- [x] 草稿阶段动态引用模板和标签，不创建收件人快照、不计算或保存收件人数。
- [x] `name`、`email` 在后续合并时来自客户；`store_name`、`sender_name`、`discount`、`product`、`order_number` 属于活动级变量。
- [x] 草稿允许变量缺失或为空；完整性校验延后到预览、导出和发送前。
- [x] 已归档模板不能用于新活动或切换引用；既有草稿可以保留该引用并继续保存，后续预览、导出和发送必须要求恢复或替换模板。
- [x] P3-1 不实现活动页面、收件人筛选、前三位预览、CSV、活动/收件人快照、ESP、队列、发送或 webhook。

## P3-1 数据库基础

- [x] 新增 `edm.campaigns`，包含工作区、名称、模板、受众、标签、活动变量、状态、归档、操作者、时间和乐观并发版本。
- [x] 模板、标签、创建者、更新者均使用工作区复合外键，阻止跨工作区引用。
- [x] 活动变量仅接受五个固定键、字符串值和每值 200 字上限；JSON 对象同时受 8KB 兜底限制。
- [x] 建立活动列表、模板、标签、创建者和更新者索引；启用 RLS。
- [x] `authenticated`、`anon` 与 `aigc_api` 均不能直接读写活动表；客户端只经过受控 RPC。
- [x] `edm_private` 保存提权实现，`edm` 仅暴露调用者权限包装；公开与匿名角色不能执行新函数。

## P3-1 业务接口

- [x] `list_campaigns`：工作区成员可读，每页 20 条，返回模板/标签/创建者摘要，不返回变量、正文或虚构收件人数。
- [x] `get_campaign`：仅管理员和编辑者读取完整草稿及当前模板内容。
- [x] `save_campaign`：仅管理员和编辑者创建或更新草稿，校验乐观并发、归档状态、状态机、变量和工作区关联。
- [x] `set_campaign_archived`：仅管理员和编辑者归档或恢复草稿，保留数据且不提供硬删除。
- [x] 新增、编辑、归档和恢复均在同一数据库事务中写入业务审计；审计只记录关联标识和变量键，不记录变量值或模板正文。

## P3-1 应用工程

- [x] 新增 `src/features/campaigns/model.ts`，统一活动输入 Zod 规则、固定变量和返回类型。
- [x] 新增 `src/features/campaigns/actions.ts`，实现工作区复核、角色检查、RPC 调用及活动/总览/日志缓存刷新。
- [x] 数据库类型生成脚本和本地认证接口已登记四个活动 RPC。
- [x] 审计日志动作增加活动中文标签。
- [x] P3-2 已用真实活动页面替换占位界面。

## P3-2 活动管理界面

- [x] 活动列表按未归档/已归档筛选并每页显示 20 条，展示真实状态、模板、受众、创建者和时间，不展示虚构收件人数。
- [x] 管理员和编辑者可新建、编辑、归档及恢复草稿；查看者只读活动摘要且不获得编辑选项或操作入口。
- [x] 新建活动必须手动选择使用中的模板；允许在没有客户时保存草稿，也允许活动变量暂时留空。
- [x] 支持全部客户或标签受众；编辑选项 RPC 仅返回当前工作区所需的模板摘要、活动变量键和标签，不返回模板主题或正文。
- [x] 变量表单根据所选模板动态生成；店铺名称和发件人分别默认取工作区名称和当前成员名称，切换模板时清除不再使用的变量。
- [x] 既有草稿引用的归档模板保留在编辑器内并显示警告，可继续保存当前引用，但不能用于新活动或切换引用。
- [x] 新增和编辑使用移动端底部弹层，具备滚动高度限制、关闭焦点恢复和 390×844 窄屏无横向溢出验证。
- [x] 列表和表单处理加载、空状态、保存错误及乐观并发重新加载提示；P3-2 不实现收件人计算、预览、CSV 或发送。

## P3-3 动态收件人与预览

- [x] 新增动态预览 RPC，每次按最新联系人、订阅及独立抑制状态重新计算，不保存收件人数、不创建活动或收件人快照。
- [x] 合格收件人必须是未归档、订阅状态为 `subscribed` 且没有独立抑制记录的联系人；标签受众还必须具有活动当前选择的标签。
- [x] 排除人数按“已归档、已抑制、非订阅”互斥归类，优先级为已归档、已抑制、非订阅，合计可与目标及合格人数对账。
- [x] 从模板主题和正文提取变量；活动级变量缺失或为空时阻断合并样本，但仍返回动态人数与缺失变量列表。
- [x] 归档模板阻断预览，要求恢复或替换模板；归档活动和非草稿状态不能进入预览流程。
- [x] 合格联系人按创建时间和标识稳定排序，最多返回前三位；`name` 为空时使用邮箱前缀，模板变量按纯文本安全替换。
- [x] 管理员和编辑者可打开及刷新预览；查看者仅保留活动摘要，不获得预览 RPC 权限或界面入口。
- [x] 预览底部弹层展示目标、合格、三类排除、阻断原因、计算时间、模板版本和前三位合并结果，并提供编辑活动或模板入口。
- [x] 异步刷新防止旧请求覆盖新结果，关闭后恢复触发按钮焦点；移动端弹层具备安全高度、滚动和无横向溢出布局。
- [x] P3-3 不写预览审计、不导出 CSV、不冻结版本；这些职责保留给 P3-4 的确认与导出事务。

## P3-4 活动确认、快照与 CSV

- [x] 仅管理员和编辑者可以确认、复制及导出；查看者不获得操作入口，数据库 RPC 也拒绝其请求。
- [x] 仅完整的未归档草稿可以确认；归档模板、活动变量缺失、零位合格收件人或超过 10000 位收件人均在事务内阻断。
- [x] 确认事务锁定活动并共享锁定模板，以同一个物化数据边界计算受众、写入活动快照和收件人快照，再将状态从 `draft` 更新为 `confirmed`。
- [x] 活动快照冻结活动名称、模板标识/名称/主题/正文/版本、活动变量、受众规则、标签名称、确认人和确认时间。
- [x] 收件人快照稳定保存联系人标识、邮箱、姓名及已合并的主题和正文；不保留联系人外键，后续客户、标签或模板变化不影响历史结果。
- [x] 活动和收件人快照均有不可更新、不可删除触发器；确认后草稿编辑 RPC 拒绝修改。
- [x] 活动行锁、活动唯一快照约束和已确认快速返回共同保证重复或并发确认只产生一套快照；活动和模板版本变化要求重新预览。
- [x] CSV 固定为 `email,name,subject,body` 四列，采用 UTF-8 BOM、CRLF 和完整双引号转义，并以 500 行为一批流式读取冻结结果。
- [x] 所有 CSV 字段统一防护 `=`、`+`、`-`、`@`、制表符和回车开头的表格公式注入风险。
- [x] 已确认活动显示冻结人数、确认时间、确认人和模板版本，可重复下载同一快照；归档后仍可查看和下载。
- [x] 已确认活动可复制为新草稿；原模板已归档时必须选择另一套使用中的模板，复制使用冻结的受众规则和活动变量。
- [x] 确认、导出、复制和归档恢复均记录业务审计；审计仅含对象标识、数量、模板版本和变量键，不写正文、变量值或完整客户数据。

## 验证与隔离

- [x] PGlite 使用全部真实迁移验证管理员、编辑者、查看者、匿名、非成员与 `aigc_api` 权限。
- [x] 覆盖变量形状、跨工作区外键、收件范围、版本冲突、归档活动、非草稿状态和归档模板引用规则。
- [x] 覆盖活动审计恰好一次且不记录敏感正文；确认 P3-1 未创建收件人或活动快照表。
- [x] 本地单元/数据库测试通过。
- [x] 完整 lint、类型、格式、端到端与生产构建验证。
- [x] 仅应用新增迁移 `20260910135553_p3_campaign_foundation`、`20260910144358_p3_campaign_editor_options` 和 `20260910223857_p3_campaign_preview` 到 content-up，未重放或改写其他迁移。
- [x] 云端复核表、约束、索引、RLS、授权、RPC、Advisor 及 AIGC 指纹不变。

本地验证：49 项单元/数据库测试和 5 项端到端测试通过；lint、类型检查、格式检查和生产构建通过。除 P3-3 场景外，覆盖动态名单变化、退订/抑制保护、精确冻结、重复与并发确认、活动/模板版本冲突、10001 人上限、历史不可变、跨工作区、查看者权限、归档模板替换复制，以及 CSV BOM、引号、逗号、换行、中文和公式注入防护。云端快照表为空，RLS、不可变触发器和 RPC 权限均已复核；匿名确认请求返回 HTTP 401 / PostgreSQL 42501。Advisor 未发现新增 EDM 安全问题；新索引暂无使用统计属于预期信息提示，既有共享项目提示保持不变。

P3-4 使用同批次迁移前后的相同 AIGC 结构查询；表、字段、策略和函数共 62 个组成部分，指纹均为 `1029237874df0991da81b78cbad0027c`。

## 后续顺序

P3 已完成；P4 首家 ESP 已确定为阿里云邮件推送 DirectMail。P4-0 适配边界与 P4-1 工作区通道配置已进入实现，详见 `development-checklist-p4-delivery.md`；真实账号验证、测试信、DNS、发送队列、worker、幂等和异常恢复后续分批完成。
