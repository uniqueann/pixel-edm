# P3 活动管理交付清单

更新日期：2026-09-10。P3-0 设计基线、P3-1 活动草稿基础和 P3-2 活动管理界面已完成工程实现、本地验证和云端数据库迁移；后续 P3 子批次在同一清单继续追加。

## P3-0 设计基线

- [x] 活动状态预留 `draft`、`queued`、`sending`、`completed`、`completed_with_errors`、`failed`；P3-1 只允许业务 RPC 操作 `draft`。
- [x] 管理员和编辑者可创建、读取完整草稿、编辑、归档及恢复；查看者只能读取不含变量和邮件正文的活动摘要。
- [x] 草稿阶段动态引用模板和标签，不创建收件人快照、不计算或保存收件人数。
- [x] `name`、`email` 在后续合并时来自客户；`store_name`、`sender_name`、`discount`、`product`、`order_number` 属于活动级变量。
- [x] 草稿允许变量缺失或为空；完整性校验延后到预览、导出和发送前。
- [x] 已归档模板不能用于新活动或切换引用；既有草稿可以保留该引用并继续保存，后续预览、导出和发送必须要求恢复或替换模板。
- [x] P3-1 不实现活动页面、收件人筛选、前三位预览、CSV、活动/收件人快照、ESP、队列、发送或 webhook。

## P3-1 数据库基础

- [x] 新增 `edm.campaigns`，包含工作区、名称、模板、受众、标签、活动变量、状态、归档、操作者、时间和乐观并发版本。
- [x] 模板、标签、创建者、更新者均使用工作区复合外键，阻止跨工作区引用。
- [x] 活动变量仅接受五个固定键、字符串值和每值 200 字上限；JSON 对象同时受 8KB 兜底限制。
- [x] 建立活动列表、模板、标签、创建者和更新者索引；启用 RLS。
- [x] `authenticated`、`anon` 与 `aigc_api` 均不能直接读写活动表；客户端只经过受控 RPC。
- [x] `edm_private` 保存提权实现，`edm` 仅暴露调用者权限包装；公开与匿名角色不能执行新函数。

## P3-1 业务接口

- [x] `list_campaigns`：工作区成员可读，每页 20 条，返回模板/标签/创建者摘要，不返回变量、正文或虚构收件人数。
- [x] `get_campaign`：仅管理员和编辑者读取完整草稿及当前模板内容。
- [x] `save_campaign`：仅管理员和编辑者创建或更新草稿，校验乐观并发、归档状态、状态机、变量和工作区关联。
- [x] `set_campaign_archived`：仅管理员和编辑者归档或恢复草稿，保留数据且不提供硬删除。
- [x] 新增、编辑、归档和恢复均在同一数据库事务中写入业务审计；审计只记录关联标识和变量键，不记录变量值或模板正文。

## P3-1 应用工程

- [x] 新增 `src/features/campaigns/model.ts`，统一活动输入 Zod 规则、固定变量和返回类型。
- [x] 新增 `src/features/campaigns/actions.ts`，实现工作区复核、角色检查、RPC 调用及活动/总览/日志缓存刷新。
- [x] 数据库类型生成脚本和本地认证接口已登记四个活动 RPC。
- [x] 审计日志动作增加活动中文标签。
- [x] P3-2 已用真实活动页面替换占位界面。

## P3-2 活动管理界面

- [x] 活动列表按未归档/已归档筛选并每页显示 20 条，展示真实状态、模板、受众、创建者和时间，不展示虚构收件人数。
- [x] 管理员和编辑者可新建、编辑、归档及恢复草稿；查看者只读活动摘要且不获得编辑选项或操作入口。
- [x] 新建活动必须手动选择使用中的模板；允许在没有客户时保存草稿，也允许活动变量暂时留空。
- [x] 支持全部客户或标签受众；编辑选项 RPC 仅返回当前工作区所需的模板摘要、活动变量键和标签，不返回模板主题或正文。
- [x] 变量表单根据所选模板动态生成；店铺名称和发件人分别默认取工作区名称和当前成员名称，切换模板时清除不再使用的变量。
- [x] 既有草稿引用的归档模板保留在编辑器内并显示警告，可继续保存当前引用，但不能用于新活动或切换引用。
- [x] 新增和编辑使用移动端底部弹层，具备滚动高度限制、关闭焦点恢复和 390×844 窄屏无横向溢出验证。
- [x] 列表和表单处理加载、空状态、保存错误及乐观并发重新加载提示；P3-2 不实现收件人计算、预览、CSV 或发送。

## 验证与隔离

- [x] PGlite 使用全部真实迁移验证管理员、编辑者、查看者、匿名、非成员与 `aigc_api` 权限。
- [x] 覆盖变量形状、跨工作区外键、收件范围、版本冲突、归档活动、非草稿状态和归档模板引用规则。
- [x] 覆盖活动审计恰好一次且不记录敏感正文；确认 P3-1 未创建收件人或活动快照表。
- [x] 本地单元/数据库测试通过。
- [x] 完整 lint、类型、格式、端到端与生产构建验证。
- [x] 仅应用新增迁移 `20260910135553_p3_campaign_foundation` 和 `20260910144358_p3_campaign_editor_options` 到 content-up，未重放或改写其他迁移。
- [x] 云端复核表、约束、索引、RLS、授权、RPC、Advisor 及 AIGC 指纹不变。

本地验证：35 项单元/数据库测试和 5 项端到端测试通过；lint、类型检查、格式检查和生产构建通过。端到端覆盖无客户建草稿、手动模板选择、动态变量默认值与切换、标签受众、编辑、归档恢复、归档模板兼容、查看者只读和移动端布局。云端活动表仍有 13 项约束、7 个索引和 1 条 RLS 策略；登录角色通过五个受控 RPC 操作活动，但不能直接读写活动表。匿名 Data API 调用活动编辑选项返回 HTTP 401 / PostgreSQL 42501。Advisor 未发现新活动对象安全或性能问题，既有空表索引未使用信息提示保持不变。

本批 AIGC 结构指纹口径包含 `aigc` 的表、字段、策略和函数；迁移前后均为 62 个组成部分，指纹均为 `1e4d0de887b08979e2ef6e51b40e8277`。

## 后续顺序

P3-2 已完成；下一批 P3-3 实现动态收件人筛选、变量完整性校验与前三位预览，P3-4 冻结活动/收件人快照并导出安全 CSV。归档模板的草稿可继续编辑，但进入 P3-3/P3-4 前必须恢复或替换模板。
