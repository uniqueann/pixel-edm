# 卖家邮局 · 开发计划 v0.1

创建日期：2026-09-09；更新日期：2026-09-21。依据：`architecture-draft-v1.3.md`；已补充核对用户提供的 `1-seller-post-office-premium.html` 交互原型。

本文记录实施方案，未确认的产品取舍仍保持待讨论状态。原架构草案保留不变。P1 工程与身份、P2 客户与模板、P3 活动管理 A 版和 P4 单通道发送工程交付均已完成；用户于 2026-09-10 确认 Google 登录、邮箱重置均已验证通过。P4 首家 ESP 为阿里云邮件推送 DirectMail；P4-0 至 P4-3 的适配边界、通道配置、内部测试发送、正式活动持久队列、限速、重试与人工核对均已实现并部署，逐项记录见 `development-checklist-p4-delivery.md`。管理员已收到测试信，通道已进入 `verified`；P4-3 正式活动发送真实验收已通过。P11-0 至 P11-3 已完成，Dodo 测试模式支付已由用户验证，正式商品、Webhook、Vercel Production 配置和重新部署已完成；P11-4 正式付款与回调闭环仍待重新验收。

已确认部署约束：复用 Supabase **content-up**（项目引用 `gnrhyahjegvcicektebh`），EDM 业务数据使用 **`edm` schema**。第一批可勾选任务及共享项目边界见 [第一批开发清单](development-checklist-p1.md)。此约束优先于本文先前的独立项目与注册初始化假设。

## 1. 当前基线与交付目标

项目启动时仓库只有 README 和架构草案；目前已有应用、数据库迁移及测试。用户提供的完整单文件交互原型（931 行 HTML/CSS/JavaScript）已阅读并归档至 `references/seller-post-office-premium.html`。应用外壳已完成本地交互验证；原型在原宿主环境中的逐项交互回归仍未执行。

原型已明确七个页面、视觉变量、表单和业务主流程；可以复用设计及交互规则。其直接操作 DOM、共享数组持久化、模拟身份和统计的实现需要迁移为组件、真实身份及服务端业务流程，不能当作已完成的产品后端。

产品定位：以工作区为边界，帮助个人和团队管理订阅客户、邮件模板及营销活动，使用用户自己的 ESP 账号发信。

分两次形成可验收产品：

- **A 版：管理闭环。** 注册或使用已有账号登录 → 首次进入 EDM 自动初始化个人工作区 → 导入客户 → 编辑模板 → 创建活动 → 预览合并内容 → 导出 CSV。具备数据库权限和审计，适合内部试用。
- **B 版：发信闭环。** 管理员连接一个真实 ESP → 测试发信 → 确认活动 → 分批发送 → 回执统计 → 退订及抑制再次发送。加入完整团队邀请和角色管理，达到受控试运行标准。

A 版可先验收；不能将它视为原草案全部完成。

## 2. 范围和建议默认值

| 领域 | 首版纳入 | 暂缓 |
| --- | --- | --- |
| 身份 | 邮箱密码、邮箱验证、重置密码、Google OAuth | 更多登录方式 |
| 工作区 | 注册自动创建、切换、个人转团队、三种角色 | 复杂组织层级、独立计费体系 |
| 客户 | 列表、搜索、分页、标签、编辑、CSV 导入导出、订阅状态 | 外部店铺同步、复杂自动分群 |
| 模板 | 主题、正文、有限变量、预览、复制 | 拖拽邮件设计器、AI 生成 |
| 活动 | 草稿、标签选人、变量校验、预览、CSV、真实发送、结果 | 定时营销、自动化流程、A/B 测试 |
| 通道 | 阿里云 DirectMail 单通道，工作区自有账号，保留扩展接口 | 同时实现其他 ESP、平台公共测试配额 |
| 团队 | 邀请、接受、撤销、角色修改、移除、操作日志 | 自定义权限策略 |
| 统计 | 接受、送达、失败、退信、投诉、打开、点击 | 收入归因、复杂报表 |

依据原型：模板采用主题与纯文本正文编辑，保留变量快捷插入；真实邮件的最小 HTML 包装由渲染器生成，不额外设计富文本或拖拽编辑器。首版标签筛选支持单标签或全部客户。首个 ESP 待用户选择，其余领域可以先开发。

技术栈沿用草案：Next.js App Router、TypeScript、Tailwind CSS、shadcn/ui、lucide-react、TanStack Query、React Hook Form、Zod、Supabase，部署目标为 Vercel。实际版本及兼容性在初始化时核定并提交锁文件。

## 3. 开发前必须补齐的设计

### 3.1 数据关系与约束

- 使用 `edm.members` 作为 EDM 应用用户表（替代先前计划中的 `edm.profiles`），包含 `user_id`、应用级 `status`、显示名和时间字段，关联共享 `auth.users.id`。不复用 `aigc.members` 的资格或 content-up 的 `public.profiles` 额度。`edm.workspace_members` 单独管理用户在各工作区的角色。补全 `edm.workspaces.owner_id`。店铺名称归工作区，具体发件人归通道，避免重复来源。
- EDM 迁移和应用不得修改或依赖 `aigc` 的业务表、策略、函数及专用角色。应用级停用仅作用于 `edm.members`；所有 EDM 业务入口与 RLS 校验其 active 状态，不能通过再次初始化恢复 disabled 用户。共享 Auth 账号的删除或全局封禁不作为 EDM 停用实现。
- 共享项目下，首次进入 EDM 调用经过身份验证的幂等数据库事务，原子创建 EDM 资料、个人工作区与 admin 成员；替代草案为所有 Auth 新用户追加全局注册触发器的方案。既有用户也走同一入口，不能依赖“本次新注册”判断。
- 明确 owner 与 admin 的关系：建议 owner 必须是活动管理员；转移所有权与调整成员角色在一个事务内完成。
- `workspace_members` 保留已加入成员；建议新增 `workspace_invitations`，存邀请邮箱、角色、令牌摘要、过期时间、接受/撤销时间，避免把未注册邀请当成有效成员。
- 增加工作区内规范化邮箱唯一约束、成员唯一约束、活动收件人唯一约束；定义必填、默认值、枚举或检查约束、`updated_at` 和查询索引。
- 使用组合外键或等效数据库约束，确保活动的模板、联系人和通道属于同一个工作区。仅验证每个 UUID 存在不够。
- 活动排队时保存主题、正文、发件人、变量、收件邮箱和必要联系人字段的快照；后续编辑模板或联系人不能改写历史发送记录。
- 明确删除行为：已发送活动保留历史；模板和联系人采用归档或可空引用与快照策略。抑制记录独立保存，不能通过删掉联系人再导入绕过退订。

### 3.2 权限与服务端边界

草案 SQL 是概念示例，不能直接复制成生产迁移：

- `FOR INSERT, UPDATE, DELETE` 不是有效的 `CREATE POLICY` 写法。分别建立策略；INSERT 使用 `WITH CHECK`，UPDATE 明确旧行和新行约束。
- 默认宽松策略会以 OR 合并，多加一条看似严格的 UPDATE 策略不一定能收紧已有授权。逐表统一设计后做越权测试。
- 只限制 `status = 'sent'` 仍允许编辑者写入 `sending`。普通用户只允许编辑草稿业务字段；排队和结果字段只能由受控服务端流程修改。
- 对 `workspace_members` 直接套用查询自身的角色函数，需要处理策略递归。若确需提权函数，应放在私有 schema，固定搜索路径、限定调用权限并校验当前用户。
- 所有客户端可访问表均启用 RLS，并分别核对表授权；服务端特权密钥不进入浏览器，特权操作仍须验证调用者和工作区。
- “至少一个管理员”的检查必须在事务内串行化同一工作区的成员变更，避免两名管理员同时降级导致全部失去管理权限。
- 审计由数据库记录业务修改，actor 使用已验证身份；后台事件标识为系统行为，不记录密钥和完整邮件正文。

建议权限矩阵如下；收件明细沿用草案的 admin 限制，其他角色只看工作区内活动汇总。注意：原型允许所有角色导出活动 CSV、查看日志和修改发件人设置；下表对导出及日志的收紧是待确认建议，不能视为已有需求。设置写权限建议限 admin，定稿时一起确认：

| 操作 | admin | editor | viewer |
| --- | --- | --- | --- |
| 查看客户、模板、活动汇总 | 是 | 是 | 是 |
| 编辑客户、模板、活动草稿 | 是 | 是 | 否 |
| 导入客户、导出 CSV | 是 | 是 | 否 |
| 配置通道、测试发信、正式发送 | 是 | 否 | 否 |
| 查看逐收件人发送明细 | 是 | 否 | 否 |
| 邀请和管理成员、查看审计日志 | 是 | 否 | 否 |
| 修改发送结果、审计记录、直接读取密钥 | 否 | 否 | 否 |

### 3.3 发送任务与统计

建议从第一版真实发送开始使用持久化队列，具体采用 Supabase Queues 或数据库任务表在通道验证阶段确定。HTTP 请求只做授权、验证、创建任务并返回；worker 有界分批处理。

- 活动状态建议为 `draft → queued → sending → completed / completed_with_errors / failed`；活动完成表示处理结束，不等于所有邮件送达。
- 收件人任务状态与互动事件分开：任务可为 pending、processing、accepted、failed、skipped、unknown；送达、硬退信、投诉、打开和点击使用独立时间字段或事件表。
- 保留 provider message ID、尝试次数、下次尝试时间、领取租约和错误分类；按工作区/通道限速。
- 重复点击发送不能重复建任务；worker 竞争领取须原子化。明确可重试与不可重试错误、最大次数和人工处理入口。
- ESP 已接受但数据库回写失败时，不能简单重发。优先利用通道幂等或查询能力；无法确认的记录进入 unknown 等待核对，不宣称端到端“严格只发送一次”。
- webhook 验证来源，按通道和 provider message ID 定位记录，事件去重并兼容乱序；事件早于发送结果落库时保留待关联记录。
- 指标分别统计 ESP 接受、实际送达与最终失败；打开/点击按唯一收件人去重。先声明分母，缺少支持的事件显示不可用，不能用随机值或假零替代。
- 打开/点击只能表达收到的追踪事件，不保证等于真实人工阅读；确定首家 ESP 后再核对其追踪能力和统计限制。

Supabase Edge Functions 有运行时限，因此不把“几百人同步循环应该够用”作为可靠性前提。官方依据见文末。

### 3.4 订阅状态、密钥和邀请

- 导入来源不等于订阅同意证明。导入流程要求说明名单来源及可用的同意记录，不能把导入时间自动冒充同意时间。
- 队列生成和每封实际发送前都检查抑制状态；重新导入不能覆盖已有退订、投诉、硬退信记录。记录发送检查与并发退订的时间边界，已提交 ESP 的邮件无法保证撤回。
- 提供无需登录的不可猜测退订链接及受控退订接口；页面 GET 展示确认，避免链接扫描直接触发操作。是否实现一键退订邮件头和对应 POST 按目标邮箱平台及首个 ESP 要求核定。
- 退订、投诉和硬退信进入工作区级抑制表；订阅信息的证据字段与抑制原因分别留存。
- ESP 配置拆分公开元数据与服务端秘密引用；管理界面仅显示配置状态及掩码。密钥写入、轮换、删除和解密只允许受控服务端执行。
- 邀请绑定工作区、邮箱、目标角色和过期时间；接受时核验已验证邮箱，一次性消费令牌。被移除成员立即失去数据访问权限。
- 这些是技术设计要求，不代表已完成任何地区的法律合规认证；面向哪些市场及数据保留规则在公开上线前确定。

## 4. 分阶段工作包及验收

| 阶段 | 工作内容与交付物 | 验收门槛 | 依赖 |
| --- | --- | --- | --- |
| P0 设计定稿 | 基于已提供原型完成页面/弹层映射、视觉基线、A/B 范围、权限差异决策、状态机、表约束、首家 ESP 候选 | 关键设计有书面结论；原型来源已解决，权限差异仍待定稿 | 本计划讨论与原型 |
| P1 工程与身份 | 项目骨架、CI、环境配置示例、Supabase 本地环境、edm 迁移、邮箱及 Google 登录、首次使用初始化、工作区切换、RLS 基线 | 新老账号首次进入 EDM 恰好初始化一个个人工作区；重复/并发初始化不产生脏数据；两个工作区不可互访；类型检查和构建通过 | 第一批清单内的基础设计；营销细节不阻塞 |
| P2 客户与模板 | 客户增改查、标签、保留粘贴导入并新增 CSV 文件导入、校验/去重/错误报告、订阅与抑制模型、纯文本模板编辑、变量快捷插入、六套默认模板、业务审计 | 重复导入不新增重复邮箱；退订不被覆盖；变量缺失可定位；默认模板仅初始化一次；编辑者和只读角色权限正确 | P1 |
| P3 活动管理（A 版） | P3-0 固化设计与权限；P3-1 建立草稿数据/RPC；P3-2 实现管理界面；P3-3 实现动态收件人及前三位预览；P3-4 冻结快照并导出 CSV | 注册到活动导出整条流程通过；未连接 ESP 仍可导出；CSV 保留 email/name/subject/body 四列；字段、编码及公式注入处理有测试 | P2 |
| P4 单通道发送 | ESP 接口验证、凭据存取、发件身份检查、内部测试信、排队、worker、限速、幂等和异常恢复 | 受控邮箱实际收到；重复提交不重复建任务；超时/限流/崩溃可恢复或进入明确待核对状态；编辑者不能发送 | P3，ESP 账号及已验证发件身份 |
| P5 回执与退订 | webhook 鉴权/去重/关联、发送明细、汇总统计、退订入口、退信/投诉抑制 | 重放和乱序事件不破坏结果；退订者后续不再入发送范围；接受与送达明确区分；只有真实事件进入统计 | P4 |
| P6 团队协作 | P6-0 规则确认；P6-1 数据库与权限；P6-2 团队管理界面；P6-3 邀请接受流程；P6-4 审计与完整验收 | 邀请不可冒领和复用；最后管理员保护通过并发测试；被移除成员失权；跨工作区缓存不串数据 | P1；可与 P4/P5 并行 |
| P7 发布验收（B 版） | 完整业务回归、队列负载验证、错误监测、迁移及恢复演练、部署配置和运维说明 | 一组受控试运行名单完成发送/回执/退订闭环；权限测试通过；无明文密钥泄漏；告警和故障处理可用 | P4、P5、P6 |
| P8 多 ESP 支持 | P8-0 契约定稿；P8-1 数据库去耦；P8-2 适配器层与 worker 分发；P8-3 配置界面动态化；P8-4 多 ESP 验收 | 两家厂商各完成真实发送/回执/退订闭环；跨厂商事件不错配；切换主通道不影响在途活动；DirectMail 无回归 | P5；P8-1 / P8-2 先于 P7，P8-3 / P8-4 在 P7 之后 |

关键路径：P0 → P1 → P2 → P3 → P4 → P5 → P8-1 → P8-2 → P7。P6 在 P1 后可独立推进，但团队版验收必须完成它。P8-3 配置界面与 P8-4 多厂商真实验收在 P7 之后推进。

排期在 P0 后结合原型迁移范围、首家 ESP 和实际投入确定；原型来源已明确。ESP 开通、DNS 验证、额度审批、Google OAuth 配置分别记录为外部等待，不能混入纯开发工时。

## 5. 页面与代码组织建议

页面：登录/注册/重置密码、工作区概览、客户列表及导入、模板列表及编辑、活动列表/创建/详情、通道与工作区设置、团队成员、操作日志、邀请接受、公开退订页。

概览放在统计口径确定之后完善；开发时先交付核心表单和主流程。

代码按业务组织：`features/contacts`、`features/templates`、`features/campaigns`、`features/workspaces`；共享界面放 `components`，Supabase 客户端及邮件渲染放 `lib`，迁移和函数放 `supabase`。

- 普通业务读写使用当前用户身份并受 RLS 约束；秘密配置、邀请接受、发送触发及 webhook 进入专门服务端边界。
- 缓存键包含 workspace ID；切换工作区清理或隔离旧缓存。角色变化后重新拉取权限。
- Zod 规则用于客户端反馈和服务端复验；数据库约束负责最终一致性。
- 邮件渲染输出 HTML 和纯文本，变量默认转义，预览隔离执行，禁止脚本及危险 URL；使用与真实发信相同的渲染路径。

## 6. 测试与发布要求

优先测试会导致越权、误发、重复发送和历史污染的行为：

1. 数据库：双工作区隔离、各角色操作、跨工作区外键、首次使用初始化事务、邀请并发、最后管理员并发保护；验证共享项目既有注册触发器未被改写。
2. 业务：邮箱去重、CSV 错误行和公式注入、变量转义、抑制不可被导入覆盖、活动快照不可被后续编辑改变。
3. 发送：重复请求、worker 租约恢复、限流重试、永久失败、ESP 接受后回写失败、待发期间退订、webhook 验签/去重/乱序。
4. 端到端：A 版完整管理流程；B 版管理员真实测试发送、编辑者越权拒绝、退订后重新建活动排除该邮箱。

每阶段完成类型检查、构建及相关测试；P7 验证公开回调 URL、日志脱敏、失败任务告警、积压监测、数据库备份和恢复流程。大批量测试使用模拟通道，真实投递仅使用自有或明确授权的测试邮箱。

## 7. 待用户确认的输入

| 输入 | 当前建议/假设 | 最晚需要时间 |
| --- | --- | --- |
| 原型代码、截图或设计文件 | 已收到完整 HTML 源码；按第 9 节作为视觉和交互基线 | 已解决来源，P0 补浏览器核验 |
| 首个 ESP | 优先用户已有且可验证发件身份的账号，仅实现一家 | P4 开始前；P0 尽早选择 |
| 首批用户、主要发送市场、名单规模 | 暂按邀请制、小规模试运行，具体人数和每日量待定 | P0 容量及验收定稿前 |
| 模板编辑深度 | 已从原型明确为纯文本正文及变量快捷插入，不含拖拽 | 已明确；若扩展需单独调整范围 |
| 原型与建议权限差异 | 日志已定为管理员可见，设置仅管理员可改；查看者导出权限在 P3 定稿 | P3 导出实现前 |
| 云端项目与部署归属 | Supabase 使用 content-up，业务 schema 为 edm；Vercel 项目 pixel-edm，正式域名 https://edm.contentup.cc | 已配置并完成首次生产部署 |

上述输入未齐时可以继续工程和数据设计；涉及对应产品取舍时需保留明确未决状态。

## 8. 本次核对的官方资料

- [PostgreSQL CREATE POLICY](https://www.postgresql.org/docs/current/sql-createpolicy.html)：策略语法、USING/WITH CHECK 和宽松策略 OR 合并规则。
- [Supabase Edge Functions 运行限制](https://supabase.com/docs/guides/functions/limits)：请求及 worker 的执行时限，是分批异步发送建议的依据。
- [Supabase Queues](https://supabase.com/docs/guides/queues)：持久化任务方案候选；不把队列保证等同于外部 ESP 投递恰好一次。

使用 Supabase 技能核查后，计划额外纳入表授权与 RLS 分别验证、服务端秘密隔离、提权函数边界和权限回归；具体接口及配置在实施阶段按官方文档再次确认。

## 9. 交互原型核对与迁移清单

本节依据源码静态核对，不代表浏览器验证已通过。附件原始路径：`/tmp/codex-remote-attachments/01a084af-be19-73e0-a0a7-b3fdea270e44/5ED1BE67-A12E-49C1-8643-1F40E96BC0E4/1-seller-post-office-premium.html`。

### 9.1 保留的视觉与交互基线

- 布局：740px 最大内容宽度、居中单列、顶部品牌与身份条、七项横向标签导航；表单在 600px 以上部分并排，编辑使用底部弹层。
- 颜色：深蓝 `#1C2B45`、纸色背景 `#F6F6F3`、白色卡片、金色标签、绿色成功和邮戳红危险操作。
- 字体：Fraunces 标题、IBM Plex Sans 正文、IBM Plex Mono 时间和数字；实现时为中文明确适当回退字体。
- 组件：14px 卡片圆角、9px 控件圆角、细边框、浅阴影、胶囊标签、轻量 Toast、空状态和权限提示。
- 迁移为设计变量与组件；shadcn/ui 应适配这些样式。先保持原型的信息布局，桌面宽屏是否拓宽另作产品选择。
- 补充弹层焦点管理、键盘关闭与返回焦点、输入标签关联、加载/错误/空搜索结果和窄屏长文本状态。

### 9.2 七个页面的具体迁移范围

| 页面 | 原型已有 | 产品化补充或替换 |
| --- | --- | --- |
| 总览 | 客户/模板/成员数、平均打开率、最近六条动态 | 真实查询；打开率明确时间范围和分母；无回执时显示暂无数据 |
| 客户 | 搜索、单标签筛选、添加/编辑/删除、订阅状态、粘贴批量导入 | 分页、邮箱去重、正式 CSV 解析、错误报告、来源证据、抑制与恢复订阅规则 |
| 模板 | 新建/编辑/复制/删除、分类、主题、正文、七个变量快捷入口、六套默认模板 | 组件化表单、变量校验、一次性初始化模板、关联活动的删除约束 |
| 活动 | 选模板和标签、自动生成变量表单、默认落款、前三封预览、四列 CSV、模拟发送统计 | 新增草稿编辑；确认发送、快照、排队、进度、错误明细及真实统计 |
| 团队 | 添加成员、修改角色、移除、最后管理员保护、权限说明表 | 邮箱邀请与接受流程、工作区隔离、数据库并发保护、所有权规则 |
| 日志 | 时间、操作者、角色、动作、对象、按成员筛选 | 数据库审计、分页、服务端身份；成员被移除后仍可追溯历史记录 |
| 设置 | 店铺/发件人姓名、ESP 介绍、原型说明、清空演示数据 | 工作区信息、真实地址、发件身份、密钥配置、测试邮件；清空演示数据不直接迁移为生产全量删除 |

### 9.3 已发现的行为差异与开发约束

1. **存储依赖宿主环境。** `loadData/saveData` 使用 `window.storage`，不是浏览器原生持久化接口。普通浏览器缺少它时读取返回默认值、保存失败；后续演示核验需明确宿主支持或在隔离副本提供适配，不能把刷新后的初始化误判为保存成功。
2. **保存失败仍可能提示成功。** 多处先修改内存，再调用 `saveData`，未检查其布尔返回值就关闭表单并提示成功。产品表单必须等待确认；失败保留输入，乐观更新失败需回滚。
3. **身份切换是模拟器。** 产品身份来自会话，顶部改为当前用户及工作区选择；普通成员不能切换成别人的身份。
4. **角色权限有未定义部分。** CSV 导出与设置保存没有 guard，日志向所有身份可见；活动删除虽按钮始终可见，但事件内部有角色检查。定稿前明确每项产品规则，再统一按钮和后端授权。
5. **名单与历史会漂移。** 草稿创建保存人数，预览和导出却实时重新筛选；模板也每次读最新版本。草稿可以动态更新，但排队时必须冻结快照；已完成活动查看和导出使用历史快照。
6. **订单变量不是逐客户字段。** 当前只有 `name/email` 来自联系人，其余变量统一属于活动，包含 `order_number/product`。首版不宣称支持逐订单物流群发或自动弃购；保留示例模板时明确用途，物流订单模板的批量使用需约束。若需要逐订单功能，应另建订单或收件人变量映射并扩展范围。
7. **变量缺失没有阻断。** 原型会保留未解析占位符，空值也可提交。产品允许草稿不完整，但导出和发送前检查；`name` 缺失时原型使用邮箱前缀，迁移时明确提示该回退。
8. **默认模板会重复出现。** 原型只要模板数组为空就重新生成六套模板。产品仅在工作区初始化时执行一次，用户主动删空后不自动恢复；模板涵盖欢迎、弃购挽回、促销、物流通知、复购唤醒和评价邀请，营销自动化不因此进入首版范围。
9. **统计是随机生成。** `generateStats()` 不迁移进业务代码；总览当前对各活动打开率简单平均，产品建议按选定时间范围累计分子/分母计算并说明口径。
10. **通道说明包含时效性信息。** 原型里的免费额度和试用天数未经本次核实，正式设置页不能直接复用这些数字；确定首家 ESP 后查证官方信息。

### 9.4 下一轮的具体交付

2026-09-10 更新：客户管理、名单导入与订阅模型、模板管理和全部 P2 业务审计均已实现、提交并部署，P2 工程范围完成。详见三个 `development-checklist-p2-*.md` 交付记录。

P3 按可独立验收的子批次推进：

1. P3-0：固化活动权限、状态、变量来源、归档模板兼容和快照边界。
2. P3-1：活动草稿表、工作区复合外键、受控 RPC、Server Actions、类型、审计和数据库测试；不实现页面、收件人计算或快照。
3. P3-2：活动列表、新建编辑、模板/标签选择、归档恢复和移动端界面。
4. P3-3：根据最新客户、订阅与抑制状态动态计算收件人，执行变量完整性校验并预览前三位。
5. P3-4：确认时冻结活动与收件人快照，导出 email/name/subject/body 四列安全 CSV，形成 A 版管理闭环。

P3-0 至 P3-4 已于 2026-09-11 完成工程实现、本地验证、云端数据库迁移和生产部署。动态预览实时读取最新客户、订阅与抑制状态，确认后冻结活动、模板版本和逐位合并结果，并允许重复下载经过公式注入防护的 UTF-8 CSV。首个 ESP 在 P4 接入。查看者只读活动摘要，管理员和编辑者管理、预览、确认、复制及导出活动。

P4-0 至 P4-2 已于 2026-09-12 完成工程实现、Supabase 部署和真实测试信验收：每工作区保存 DirectMail 安全摘要与 AES-GCM 加密凭据，管理员可向自己的已验证登录邮箱发起幂等测试发送。数据库不保存完整测试收件邮箱，Edge Function 不自动重试未知结果；Vercel Production 与 Supabase Edge Functions 已同步密钥环，专用 RAM AccessKey 已保存，worker 权限及阿里云 CommonJS SDK 兼容故障均已修复。管理员已收到测试信，通道已进入 `verified`。

P4-3 已于 2026-09-12 完成工程实现、Supabase 部署和正式发送真实验收：单个活动最多 500 位收件人，管理员输入完整活动名后幂等入队；worker 按工作区每秒最多 5 次、上海时区每日最多 2000 次供应商调用，临时错误按 30 秒、2 分钟、10 分钟重试，未知结果不自动重试。支持暂停/继续、通道错误自动暂停、放弃剩余待发任务和逐收件人人工核对。验收时云端 2 个正式发送运行均为 `completed`，3 个收件人任务均为 `accepted`，确认从活动入队到 DirectMail 受理的正式发送链路正常。`accepted` 仅表示供应商已受理，最终送达、退信、投诉与退订状态由 P5 回执闭环补齐。

P5-1 已于 2026-09-13 完成工程实现、Supabase 部署和真实投递成功验收：EventBridge 以 `acs.dm` 接收 DirectMail 7 类事件，通过通道令牌、60 秒时间窗、官方证书白名单和 RSA-SHA256 验签后幂等入库，并按 EnvId、Message-ID 或唯一时间候选关联发送任务。MNS 死信队列、指数退避、30 天重匹配和 180 天保留任务均已启用；两条真实投递成功事件均进入 `applied`，对应任务由 `awaiting_receipt` 自动投影为 `delivered`，其中一条同时验证了 EventBridge 失败后的自动重试恢复。

P5-2 已于 2026-09-13 完成工程实现、Supabase 与 Vercel 生产部署及受控真实退订验收：正式发送任务生成不含客户信息的 HMAC-SHA256 长期令牌，密钥环支持轮换；公开页面 GET 仅展示掩码邮箱和确认动作，RFC one-click 只接受标准表单 POST。两条入口共用服务角色事务，以发送时邮箱写入退订事件和工作区级抑制，并保持投诉优先级；发送运行冻结工作区名称与联系地址，缺失地址禁止开始发送，worker 领取时再次检查当前联系人和抑制状态。真实邮箱客户端的顶部退订入口实际以 GET 打开 API 地址，兼容修复后安全跳转到公开确认页；两个受控地址完成退订，云端事件、抑制、联系人状态和来源任务反馈投影全部一致。严格 RFC one-click POST 的真实客户端触发及退订后新活动排除保留为扩展回归项。

P5-3 已于 2026-09-14 完成工程实现、Supabase 与 Vercel 生产部署及真实闭环验收：通道可配置 DirectMail 行为追踪标签，发送运行冻结追踪配置，worker 按配置发送安全 HTML、标签和 ClickTrace 参数，并排除退订链接追踪。活动列表和明细按已受理与已送达的明确分母展示送达、退信、投诉、退订、打开和点击统计，管理员可按结果筛选逐收件人任务。验收活动 `52c921f5-862c-4b68-9154-a29ce54c0cba` 完成 1 封真实发送，投递成功、打开和点击 3 条 EventBridge 事件均精确关联并应用；用户随后通过公开确认页退订，形成单条退订事件与单条工作区抑制，联系人、来源任务和统计界面一致更新为已退订。期间发现 EventBridge 仍指向早期工作区通道，已通过令牌轮换、目标修正和仅限 3 条已知事件的定向重放完成修复，未改写其他历史事件。P5 至此完成，下一批进入 P6 团队协作。

P6-0 至 P6-3 已于 2026-09-15 完成工程实现，P6-4 已完成审计与自动化验收；生产域名下使用第二个真实账号接受邀请的人工验收仍待完成。

- P6-0：完成邀请、接受、撤销、过期、角色变更、成员移除、所有权转移和最后管理员保护规则。
- P6-1：新增 `20260915153722_p6_team_collaboration` 与 `20260915155319_p6_team_collaboration_hardening` 并应用至 content-up 云端。个人工作区仅在首条有效邀请提交的同一事务内转为团队且不可回退；数据库只保存邀请令牌的 SHA-256 摘要。成员变更使用工作区行锁和乐观版本保护，成员移除为软删除，历史业务数据不级联删除。
- P6-2：团队页按活动成员与待处理邀请分区；管理员可邀请、重发、撤销、修改角色、移除成员和转移 owner，编辑者与查看者只读；一次性链接只在创建或重发响应中显示。
- P6-3：邀请接受页保留 token 完成登录续接，仅允许已验证且邮箱匹配的账号接受；支持 7 天有效期、重发后旧令牌失效、惰性过期和一次性消费。
- P6-4：审计及自动化测试覆盖邮箱验证、邀请生命周期、角色、owner 转移、并发管理员保护、跨工作区、被移除后失权、查看者权限、审计脱敏和 AIGC 隔离。逐项记录见 [P6 团队协作交付清单](development-checklist-p6-team.md)。

P8 多 ESP 支持已于 2026-09-18 完成规划定稿，P8-1 数据库去耦已于 2026-09-19 完成并通过本地回归与云端应用；P8-2 适配器层与 worker 分发已于 2026-09-19 发布到 content-up；P8-3 配置界面按 provider 动态化已于 2026-09-19 完成工程实现。第二家 ESP 选定 Amazon SES，首版只支持阿里云邮件推送和 SES 两家，且同一工作区同时只有一个主发送通道，不做自动故障切换。

P8 规划时 `aliyun_directmail` 字面量散布在 8 个迁移的 24 处，厂商专属列和退信状态码映射直接写在共享表与 SQL 里，限速同样写死在领取 RPC 中。因此 P8-1 先完成纯前向数据库去耦；生产回填已确认 2 个通道与 8 条发送运行全部迁入 `provider_config`，DirectMail 行为保持不变。

P8-1 已由单文件迁移 `20260919021500_p8_delivery_provider_registry.sql` 完成：新增 `edm.delivery_providers` 注册表承载显示名、能力位与默认限速，两处 `provider` 单值约束换成注册表外键，厂商专属的区域与追踪标签迁入 `provider_config jsonb` 并由按 provider 分派的校验函数归一化，`sender_alias` 上限与发件域名是否必填改由注册表决定，退信分级归一化为事件表上的 `failure_class`，限速从注册表读取并支持通道级覆盖，发送运行同时冻结 provider 与 provider_config，主通道由 `workspace_id` 上的部分唯一索引保证唯一。既有 101 项自动化测试未修改即全部通过，另补 9 项针对注册表与去耦语义的测试。

P8-2 将 `DeliveryAdapter` 扩展为发送、错误分类、回执解析、Webhook 验签和能力描述五件套。DirectMail 实现平移到 provider 目录并保留旧入口；SES 使用 v2 `SendEmail` 和 SDK SigV4，发送尝试与任务显式保存 `provider_message_id`，SNS 回执不借用 EnvId 而只按 MessageId 匹配。SNS 入口校验证书来源、Topic ARN、时间窗、RSA-SHA256 签名与通道令牌，支持订阅确认和多收件人拆分，`bounceType` 由适配器直接归一化。AES-GCM AAD 增加 provider 隔离，RFC 8058 自建退订继续作为唯一事实来源。通用 worker、测试函数和云端 cron 切换迁移已就位，120 项测试及 Edge/Next 类型、Lint、格式与构建通过；生产仍保持 `amazon_ses.enabled=false`。P8-3 已完成 provider 描述符、多通道列表、主通道切换与 SES 沙箱提示；剩余新增工作是 P8-4 两家厂商真实闭环验收，逐项状态见 [P8 多 ESP 支持交付清单](development-checklist-p8-multi-esp.md)。

P11-0 至 P11-3 已完成：`20260920190000_p11_billing_foundation.sql` 已建立工作区订阅事实、支付事件幂等记录及账单状态 RPC；pixel-edm 已接入 Creem/Dodo Checkout、EDM 专用 metadata、独立子域 Webhook 和设置页升级入口。2026-09-22 已将正式价格调整为 Pro USD 14.90/月、149.90/年，Team USD 44.90/月、449.90/年；Dodo 正式环境已创建对应商品和 Webhook，Vercel Production 已使用 `live_mode`。2026-09-21 曾发现正式 Checkout 因运行时密钥配置异常返回 ByteString 错误，已重新写入正式 API Key/Webhook Key 并重新部署；该次处理未修改 content-up、主站支付、`aigc` schema、共享 Auth 触发器或本地迁移历史。P11-4 当前只保留正式支付/回调、取消/过期回退、主站隔离和 `supabase/verification.md` 记录待验收项。P11-5 订阅生命周期的取消续费、周期末降级、超额席位锁定和 80% 提示已实现：取消是「取消续费」，当期权益保留到周期结束再降 free；升档付款成功后立刻生效；超额数据只锁不删，工作区 `type` 不因降档回退。按天补差价与席位加购仍不做。2026-09-22 已把 `20260922083000_p11_subscription_lifecycle.sql` 应用到 content-up，云端记录 `20260922020435_20260922083000_p11_subscription_lifecycle`。逐项见 [P11-5 订阅生命周期](development-checklist-p11-subscription-lifecycle.md)。

这一调整减少了视觉探索和交互定义的不确定性；数据库、发送通道和服务端权限仍需完整实现，不能按原型页面已经可点击就视为完成。
