# P2 客户管理交付记录

更新日期：2026-09-10。客户管理第一部分已实现、提交并部署；云端增量迁移已应用。

## 已完成

- [x] `edm.contacts`、`edm.tags`、`edm.contact_tags`，工作区内邮箱唯一、组合外键、RLS 与受控事务入口。
- [x] admin/editor 管理、viewer 只读；停用和成员失效检查、跨工作区访问拒绝。
- [x] 列表、邮箱/姓名字面搜索、单标签筛选、正常/归档状态和每页 20 条分页，条件保存到 URL。
- [x] 新增编辑、表单内标签选择和创建、归档确认、恢复、版本冲突拒绝和失败保留输入。
- [x] 归档保留邮箱及标签，恢复后才能编辑；不提供永久删除。
- [x] 保存核对页面工作区与当前工作区，避免旧表单写入切换后的工作区。
- [x] 总览客户数显示未归档客户数量。
- [x] 数据库测试共 14 项通过（含原有测试）；两条浏览器流程通过；lint、类型检查和生产构建通过。

## 云端与验证边界

- 迁移版本：`20260910035224_edm_contacts`，本地文件已对齐云端版本。
- 三张新表均启用 RLS；迁移前后 AIGC 策略指纹一致，记录见 `supabase/verification.md`。
- 云端匿名 Data API 请求返回 401 / 42501，未暴露客户资料；没有返回未暴露 schema 的 PGRST106。
- 浏览器测试使用本地模拟认证和 PGlite 的实际 SQL/RLS，未代替真实用户完成云端客户写入验收。
- 版本冲突、事务回滚和唯一约束已有测试；未进行多连接数据库并发压测。

## 下一批

名单粘贴/CSV 导入、订阅与抑制模型已完成，详见 `development-checklist-p2-imports.md`；模板管理和全部 P2 业务审计也已完成，详见 `development-checklist-p2-templates-audit.md`。迁移前已有客户统一为“未确认”，不因既有资料自动视为已同意订阅。
