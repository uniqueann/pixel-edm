# P2 模板管理与业务审计交付记录

更新日期：2026-09-10。模板管理、默认模板初始化和全部 P2 业务审计已完成工程实现；云端数据库迁移已应用，应用代码待提交和部署。

## 模板管理

- [x] 独立 `/templates` 页面，支持使用中/已归档筛选、每页 20 条分页、新建、编辑、复制、归档和恢复。
- [x] 模板使用纯文本主题与正文，不解析 HTML；名称、分类、主题、正文长度及主题换行均有客户端、服务端和数据库约束。
- [x] 固定支持 `name`、`email`、`store_name`、`sender_name`、`discount`、`product`、`order_number` 七个变量。
- [x] 变量可插入当前主题或正文光标位置；未知变量、非法语法和未闭合双花括号会阻止保存。
- [x] 预览值可临时编辑但不入库；姓名为空时回退邮箱前缀，缺失变量会明确标记并阻止复制。
- [x] 支持复制“主题 + 正文”的完整预览，以及把现有模板复制成独立数据库模板。
- [x] 六套原型英文模板已为存量工作区初始化；未来工作区首次创建时初始化，重复登录或归档全部模板都不会重建。
- [x] 管理员和编辑者可管理模板，查看者只能查看、预览和复制预览内容。

## 业务审计

- [x] 新增只追加的 `edm.activity_logs`，记录操作者姓名与角色快照、动作、目标和有限元数据。
- [x] 客户新增/编辑/归档/恢复/退订、名单导入完成及模板全部写操作均在数据库事务内记录。
- [x] 导入任务只在首次完成时记录一条汇总日志，不逐客户刷日志；默认模板初始化每个工作区仅记录一条系统日志。
- [x] 不补造迁移前业务历史，不记录模板正文、导入原始行或退订证据全文。
- [x] 独立 `/logs` 页面按时间倒序分页并支持操作者筛选；仅管理员可见，成员后续改名或变更角色不影响历史快照。
- [x] 总览展示真实未归档模板数；管理员看到最近五条动态，其他角色不读取审计数据。

## 数据库与隔离

- 云端项目：复用 `content-up`，迁移版本 `20260910092601_p2_templates_audit`。
- 新表：`edm.templates`、`edm.activity_logs`；均启用 RLS。
- `authenticated` 只能直接读取模板，不能直接写模板或读写审计；业务写入和日志读取均经过受控 RPC。
- `anon` 不能调用模板 RPC，`aigc_api` 对新表无权限，登录用户不能调用内部审计写函数。
- 迁移后两个现有工作区各有六套活跃默认模板和一条系统初始化日志。
- `aigc` 迁移前后均为 62 个结构组成部分，指纹均为 `0ee3d68fae1719bf8ae01ed98cebdb35`。

## 验证记录

- `npm test`：27 项通过，覆盖默认模板幂等、变量约束、并发版本、角色权限、归档恢复、全部 P2 审计和审计不可伪造。
- `npm run test:e2e`：4 项通过，新增模板变量光标插入、错误提示、预览剪贴板、复制归档、日志筛选、查看者和窄屏验证。
- `npm run lint`、`npm run typecheck`、`npm run format:check`、`npm run build`：通过。
- Supabase Advisor 未发现本次新增 EDM 安全告警或缺失外键索引；新索引因刚创建尚未产生使用统计，显示为信息级提示。

## 后续边界

模板当前只保存内容定义；活动级变量值、收件人合并、发送前完整性阻断、活动快照和 CSV 导出在 P3 实现。默认模板中的弃购、物流和订单变量不表示已经具备自动化订单数据源。
