# 第一批开发清单：工程、身份与工作区

更新日期：2026-09-10。状态：P1 按用户确认收尾，下一阶段为 P2 客户与模板。工程与数据库主体、云端基础迁移、Vercel 首次部署及正式域名配置已完成。既有类型检查、构建、9 项数据库测试及一条完整本地浏览器流程通过；用户于本日确认 Google 登录和邮箱重置均已实际验证通过。本次收尾仅同步文档，不重复认证验证或修改云端配置。

清单保留尚无独立验收记录的项目，不将其自动勾选为通过；按用户本次决定，这些记录不阻塞 P1 收尾，后续在相关业务或发布验收中补充。

本地预览：http://localhost:3105。`[x]` 表示该项实现或已注明范围内的验证完成，不代表云端全流程已验收。具体验证边界见 README 和 `supabase/verification.md`。

不再单独展开完整规划阶段；按下列任务开发和验收。ESP 选择、营销统计细节与订单模板范围不阻塞这一批。

## 1. 已确定的数据库边界

- [x] 用户指定复用 Supabase `content-up`，项目引用 `gnrhyahjegvcicektebh`；只读查询确认状态为 `ACTIVE_HEALTHY`。
- [x] 用户指定业务 schema 为 `edm`；迁移前确认不存在，现已通过基础迁移创建。
- [x] 核对 `auth.users` 已有 `on_auth_user_created` 触发器，调用 `public.handle_new_user()` 创建 `public.profiles`，含免费计划和额度字段。
- [x] 明确共享项目意味着共享 Auth 用户池、身份配置与项目资源。`edm` 是业务命名空间，不是独立身份系统或安全沙箱；仍须表授权与 RLS。

EDM 的业务资料、工作区、成员，以及后续客户、模板、活动、日志等均归 `edm`。内部提权函数若需要，使用另行创建且不暴露给 Data API 的 EDM 专属内部 schema；秘密不得因为业务 schema 对外暴露而可被读取。

用户先通过现有 Auth 注册或登录，首次进入 EDM 后由已验证身份调用幂等数据库事务，原子创建 `edm.members`、个人工作区和管理员成员。调用方不能指定其他用户作为初始化对象；并发重复请求只产生一套初始数据。页面可以触发调用，但一致性由数据库保证。

### 应用用户与 AIGC 隔离（已补充只读核对）

实际 `aigc.members` 包含 `user_id`（主键，引用 `auth.users.id`）、`status`（active/disabled）和 `created_at`。其读取策略面向专用角色 `aigc_api`，使用 `aigc.user_id` 会话设置确定用户。因此 EDM 不复制或更改这套角色、会话变量和授权机制。

EDM 用户设计：

| 表 | 责任 |
| --- | --- |
| 共享 `auth.users` | 登录身份、密码及认证；不表示自动拥有任一应用业务权限 |
| `edm.members` | EDM 用户资格及资料：user_id、status、display_name、created_at、updated_at；替代此前计划的 edm.profiles，不额外重复创建资料表 |
| `edm.workspace_members` | 工作区归属与 admin/editor/viewer 角色，引用 EDM 用户和工作区 |
| 既有 `aigc.members` | AIGC 用户资格，保持独立 |

- [x] EDM 业务授权同时检查 `edm.members.status='active'` 和工作区角色；客户端不得自行修改应用级 status。初始化不得把 disabled 改回 active。
- [x] EDM 停用仅更新本应用用户资格；不删除共享 Auth 用户，不全局封禁账号，不修改 AIGC 用户状态。
- [x] 不增删改 `aigc` 表、数据、RLS、函数、`aigc_api` 角色或其权限；不设置/依赖 `aigc.user_id`。
- [x] 核查 EDM 对象的 schema/table/function 授权及默认授权，确保 `aigc_api` 不因角色继承或 PUBLIC 授权获得 EDM 业务权限；修正仅作用于 EDM 新对象，不变更既有角色。
- [ ] 云端迁移前后比对 AIGC 结构和策略；使用授权测试夹具验证仅 AIGC 成员不自动成为 EDM 工作区成员，EDM 停用不影响 AIGC 资格，反向同理。

同项目仍共用 Auth 配置、数据库算力、连接数和项目额度，schema 不能实现资源级完全隔离。EDM 采用有界查询、限流和后台批次控制；涉及共享 Auth/项目配置的修改需先评估 AIGC 兼容性。此要求不授权修改 AIGC 来适配 EDM。

既有 content-up 用户可以使用同一账号进入 EDM，再完成初始化；只在 content-up 使用服务的账号不会因新加 EDM 全局触发器被批量初始化。当前既有触发器仍会为从 EDM 新注册的 Auth 用户创建 content-up 资料和额度，这属于现有共享 Auth 行为；本批不改写原应用的额度逻辑。若未来要求两套注册用户完全隔离，需要重新讨论身份架构。

## 2. 按顺序执行的任务

### T01 工程骨架

- [x] 初始化 Next.js App Router、TypeScript、Tailwind、shadcn/ui 及计划中的表单/缓存依赖，核定兼容版本并提交锁文件。
- [x] 建立业务目录、格式化/静态检查、类型检查、构建命令、CI 和环境配置示例。
- [x] 配置浏览器端、服务端 Supabase 客户端边界；数据库查询显式指定 `edm`，生成对应 schema 类型。
- [x] 依赖安装、类型检查和生产构建通过；应用只使用公开密钥，没有服务端秘密进入仓库或浏览器包。

### T02 原型迁移基线与页面外壳

- [x] 原始 HTML 已完整归档 `references/seller-post-office-premium.html`；新界面已查看桌面登录截图与窄屏设置截图。
- [ ] 原始 HTML 依赖宿主 `window.storage`，其原宿主下的逐交互回归尚未执行。
- [x] 提取颜色、字体、圆角、阴影；完成按钮、输入、卡片、标签、弹层、Toast 和空状态组件。
- [x] 完成总览、客户、模板、活动、团队、日志、设置七页路由及导航。
- [x] 顶部显示当前账号与工作区；演示身份切换不进入产品。
- [x] 七页导航、弹层键盘关闭及焦点返回、窄屏不溢出测试通过；未完成业务显示明确待接入状态。

### T03 本地数据库与迁移边界

- [x] 配置 Supabase 本地开发与版本化迁移；业务对象显式使用 schema 限定名。
- [x] 创建 `edm`、`edm.members`、`edm.workspaces`、`edm.workspace_members`；定义外键、唯一约束、角色及时间字段。
- [x] 区分 owner 与 admin，定义个人工作区一次性初始化标识；避免依靠可变的 `type='personal'` 判断是否已初始化。
- [x] 创建通过当前用户身份调用的初始化事务和必要内部函数；限定函数权限和搜索路径。
- [x] 空库迁移、重复初始化与失败事务回滚通过。
- [ ] 完整本地 Supabase 服务验收：本机没有 Docker/Podman；目前采用 PGlite 执行数据库验证。

### T04 授权与 RLS

- [x] 制定资料、工作区、成员的最小表授权和逐操作 RLS；防止成员自提权及角色查询递归。
- [x] 当前批次只实现身份/工作区所需操作；客户导出和日志可见范围在对应业务开发前定稿。
- [x] 初始化与成员变更走受控路径；客户端不能伪造 owner、其他 user ID 或成员角色。
- [x] 验收：匿名、普通用户、其他工作区成员越权及双工作区读写隔离测试通过（PGlite）。
- [ ] 多连接并发初始化与成员变更测试；当前嵌入式数据库为单连接，未以重复调用冒充并发验证。

### T05 登录与首次进入 EDM

- [x] 完成邮箱注册、验证回调、登录、退出、忘记密码和重置密码。
- [x] 完成 Google OAuth 回调流程；已增补 `https://edm.contentup.cc/auth/callback`，保留既有地址和共享 Site URL；用户于 2026-09-10 确认真实 Google 登录验证通过。
- [x] 完成 SSR 会话刷新和服务端身份验证、登录后初始化、失败可重试页面及受保护路由。
- [x] 共用 Auth 时核对站点 URL、邮件链接、Cookie/存储命名和退出作用范围；共享 Site URL 保持 `https://contentup.cc`，EDM 使用精确回调及受控邀请回调，不以全局退出误伤其他应用会话。
- [x] 本地模拟认证协议与真实 RLS 测试通过：登录、首次进入、重复初始化和退出后访问拦截。
- [x] 云端 Google 登录及邮箱重置：用户于 2026-09-10 确认两项均已验证通过。
- [ ] 新老账号分别覆盖、注册验证邮件等扩展认证场景尚无单独验收记录；不重复要求用户验证已确认的两项流程。

### T06 工作区上下文

- [x] 加载当前用户实际加入的工作区，保存并验证当前选择；API 再校验成员身份。
- [x] 缓存键包含 workspace ID；切换、退出、成员资格失效时隔离或清理缓存。
- [x] 实现工作区名称等基础设置的管理员写权限；共享 Auth 资料与 EDM 展示资料分清归属。
- [x] 验收：用测试夹具建立两个工作区，切换后不串数据；不借此提前开放完整团队邀请功能。

### T07 content-up 云端接入与回归

- [x] 应用迁移前重新核对目标项目、`edm` 是否已被其他操作创建、相关授权和共享迁移历史。
- [x] 本仓库只维护 EDM 增量迁移；Supabase 迁移历史是项目级共享，先核对冲突，不使用远程 reset、删除其他项目迁移记录或把远端历史当作本仓库独占。
- [x] 本地验证后应用限定 EDM 对象的增量迁移；记录云端迁移版本及恢复步骤。
- [ ] Data API 暴露列表缺少单独的配置核对记录；历史“未暴露”描述不作为当前云端状态结论。后续相关 API 验收时记录实际配置，保留已有 schema，且不暴露 `edm_private`。
- [x] 后续 Edge Function 使用 `edm-` 命名前缀，环境秘密和定时任务也采用 EDM 专属名称，避免共享项目命名冲突。
- [ ] 验收：EDM 的 API 请求与 RLS 生效；比对确认既有表、注册触发器和配置未被覆盖；使用授权测试账号验证原应用登录及资料初始化可用。

### T08 第一批交付验收

- [x] 完成类型检查、构建、数据库权限/幂等测试及本地模拟认证的端到端测试；不包含云端认证验收。
- [x] 验证窄屏、键盘、加载、失败及重试状态。
- [x] README 写明本地启动、必要环境变量、共享项目迁移流程及已完成/未完成的功能。
- [x] Vercel 项目 `pixel-edm` 完成首次生产部署，正式域名为 `https://edm.contentup.cc`；Cloudflare CNAME 和生产站点环境变量已配置。
- [x] 2026-09-10 按用户要求同步 P1 文档并收尾；下一批推进 P2 客户管理、名单导入和模板管理。
- [ ] 最终演示：用户登录 → 初始化 EDM 工作区 → 七页导航 → 工作区切换 → 退出；云端与本地验证分别记录，不混报。

完成标准：第一批提供可验证的应用基础和真实身份/工作区隔离，不包含完整客户 CRUD、邮件发送或团队邀请，后续按总计划 P2 起推进。

## 3. 官方依据

[Supabase 自定义 schema 文档](https://supabase.com/docs/guides/api/using-custom-schemas)说明 Data API 暴露、schema 授权与客户端 schema 选择需要分别配置。此处按最小权限实施；独立 schema 不替代工作区 RLS。
