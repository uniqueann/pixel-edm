# P2 名单导入与订阅模型交付记录

更新日期：2026-09-10。名单导入、订阅证据与抑制保护已完成工程实现和云端数据库迁移；应用代码当前尚未提交、推送或部署。

## 已实现

- [x] 独立 `/contacts/import` 页面，支持粘贴名单和 UTF-8 CSV 文件。
- [x] CSV 使用 Papa Parse 5.7.0，支持 BOM、引号内逗号及换行；依赖使用精确版本并更新锁文件。
- [x] 固定中英文字段：邮箱、姓名、标签、订阅状态、同意来源、同意证据说明、同意时间。
- [x] 粘贴内容可带表头；无表头时按“邮箱、姓名、标签”三列处理；支持自动、逗号和制表符分隔。
- [x] 单次限制 1000 个源数据行、2 MiB；文件编码异常、未知列和 CSV 解析错误提供反馈。
- [x] 邮箱统一去空格和转小写；同一文件按邮箱合并，姓名取首个非空值，标签不区分大小写取并集。
- [x] 重复客户只补充空姓名并合并标签，不覆盖已有非空姓名；归档客户不自动恢复。
- [x] 预检区分新增、更新、无变化、归档跳过、抑制保护、写入抑制和错误，并支持 50 条分页查看。
- [x] 导入任务和行结果持久化；确认后按每批 100 条处理，刷新后可从最近 10 次导入继续。
- [x] 提供固定 CSV 模板和处理报告下载；导出单元格对公式起始字符进行转义。
- [x] 客户列表展示并筛选未确认、已订阅、已退订、硬退信和已投诉状态。
- [x] 新客户默认“未确认”；只有显式声明并提供同意来源、证据说明时才写入“已订阅”。同意时间不使用导入时间代替。
- [x] 管理员和编辑者可手动退订并填写原因；查看者不能导入或退订。
- [x] 退订、硬退信和投诉写入独立工作区级抑制记录，优先级为投诉、硬退信、退订；重新导入不会恢复订阅。
- [x] 编辑客户邮箱时，新邮箱重新进入未确认或其已有抑制状态，旧邮箱抑制记录保留。

## 数据库与权限

- 云端项目：复用 `content-up`。
- 云端迁移：`20260910081147_edm_contact_imports`。
- 新表：`edm.contact_imports`、`edm.contact_import_rows`、`edm.subscription_events`、`edm.suppressions`。
- 所有新表启用 RLS；管理员和编辑者仅有工作区内读取权限，写入集中在校验调用者和工作区的事务函数。
- `anon` 不能读取导入表或调用导入函数；`authenticated` 不能直接写导入表，也不能调用内部标签辅助函数。
- `aigc_api` 对新 EDM 对象无权限。本次迁移前后 `aigc` 均为 78 个对象组成部分，指纹均为 `583ad65cf2827de7661958675ac61e00`。

## 验证记录

- `npm test`：21 项通过，覆盖工作区授权、直接写入拦截、导入合并、归档跳过、同意事件、抑制、重新导入保护和手动退订。
- `npm run test:e2e`：3 项通过，覆盖粘贴预检/去重/错误行、CSV BOM 与引号字段、退订界面、原客户 CRUD、窄屏弹窗和七页导航。
- `npm run lint`、`npm run typecheck`、`npm run build`：通过。
- 云端检查：4 张新表、4 个客户订阅字段和 4 条读取策略存在；匿名读取、登录用户直接写入、内部辅助函数调用及 `aigc_api` 读取均为 false。
- Supabase Advisor 未发现本次新增 EDM 安全告警。现存安全提示属于共享项目既有 `public/aigc` 配置，按项目约束未在本迁移中改写。

## 本批次边界

本批次不包含真实邮件发送、公开退订链接、ESP 回执/Webhook 或重新订阅审批。后续生成收件人及实际发送时，仍须同时检查：客户未归档、状态为已订阅、邮箱不存在任何抑制记录。

